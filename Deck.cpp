

//Deck::Deck() {
    //for (int i = 9; i <= 14; i++) {
        //cards_.emplace_back(Suit::HEARTS, i);
        //cards_.emplace_back(Suit::DIAMONDS, i);
       // cards_.emplace_back(Suit::CLUBS, i);
      // cards_.emplace_back(Suit::SPADES, i);
   // }
//}
//void Deck::shuffle() {
    //unsigned seed = std::chrono::system_clock::now().time_since_epoch().count();
    //std::shuffle(cards_.begin(), cards_.end(), std::default_random_engine(seed));
    //nextCardIndex_ = 0;
//}

//Card Deck::drawCard() {
    //Card card = cards_[nextCardIndex_];
   // nextCardIndex_++;
    //return card;
//}

